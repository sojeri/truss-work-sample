const assert = require('assert');
const csvNormalize = require('../src');

describe('csvNormalize', () => {
    it('has a passing test', () => {
        assert.equal(true, true);
    });

    it('has a failing test', () => {
        assert.notEqual(index, undefined);
    });
});